#ifndef CORRELATE_HPP_
#define CORRELATE_HPP_

/* The job of the correlator is to process an incoming stream of photons,
 * and prepare all groupings of a fixed number (2 for second order, etc).
 * The output is a stream of these correlation tuples, suitable for 
 * histogramming.
 */
class Correlator
{
	
}

#endif
