#ifndef COMBINATORICS_TEST_H_
#define COMBINATORICS_TEST_H_

#include <stdio.h>
#include "../options.h"

int combinatorics_test(FILE *stream_in, FILE *stream_out,
		pc_options_t const *options);

#endif
